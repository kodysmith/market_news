"""
Market News APIs package.

This package is intentionally lightweight; `api.py` remains the primary entrypoint
for running the Flask server, while modular route blueprints live in `routes_*`.
"""

